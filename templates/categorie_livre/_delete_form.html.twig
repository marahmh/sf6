<form method="post" action="{{ path('app_categorie_livre_delete', {'idCategorieLivre': categorie_livre.idCategorieLivre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_livre.idCategorieLivre) }}">
    <button class="btn">Delete</button>
</form>
