{% extends 'base_back.html.twig' %}

{% block title %}CategorieLivre{% endblock %}

{% block body %}
    <h1>CategorieLivre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCategorieLivre</th>
                <td>{{ categorie_livre.idCategorieLivre }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie_livre.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_livre_index') }}">back to list</a>

    <a href="{{ path('app_categorie_livre_edit', {'idCategorieLivre': categorie_livre.idCategorieLivre}) }}">edit</a>

    {{ include('categorie_livre/_delete_form.html.twig') }}
{% endblock %}
