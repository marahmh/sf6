{% extends 'base_back.html.twig' %}

{% block title %}AchatRecomponse{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <h1>AchatRecomponse</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Utilisateur</th>
            <td>{{ achat_recomponse.idUtilisateur }}</td>
        </tr>
            <tr>
                <th>Recomponse</th>
                <td>{{ achat_recomponse.idRecomponse }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ achat_recomponse.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_achat_recomponse_index') }}">back to list</a>

    <a href="{{ path('app_achat_recomponse_edit', {'id': achat_recomponse.id}) }}">edit</a>

    {{ include('achat_recomponse/_delete_form.html.twig') }}
{% endblock %}
