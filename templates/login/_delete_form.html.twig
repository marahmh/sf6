<form method="post" action="{{ path('app_login_delete', {'idLogin': login.idLogin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ login.idLogin) }}">
    <button class="btn">Delete</button>
</form>
