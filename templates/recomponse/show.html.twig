{% extends 'base_back.html.twig' %}

{% block title %}Recomponse{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <h1>Recomponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRecomponse</th>
                <td>{{ recomponse.idRecomponse }}</td>
            </tr>
            <tr>
                <th>NomRecomponse</th>
                <td>{{ recomponse.nomRecomponse }}</td>
            </tr>
            <tr>
                <th>DescriptionRecomponse</th>
                <td>{{ recomponse.descriptionRecomponse }}</td>
            </tr>
            <tr>
                <th>PhotoRecomponse</th>

                <td><img src ="{{ asset('uploads/' ~ recomponse.photoRecomponse ) }}"  style = "width: 100px ;height: 100px"></td>
            </tr>
            <tr>
                <th>PrixRecomponse</th>
                <td>{{ recomponse.prixRecomponse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recomponse_index') }}">back to list</a>

    <a href="{{ path('app_recomponse_edit', {'idRecomponse': recomponse.idRecomponse}) }}">edit</a>

    {{ include('recomponse/_delete_form.html.twig') }}
{% endblock %}
