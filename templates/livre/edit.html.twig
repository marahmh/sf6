{% extends 'base_back.html.twig' %}

{% block title %}Edit Livre{% endblock %}

{% block body %}
    <h1>Edit Livre</h1>

    {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_errors(form,{'attr':{'class':'text-danger'}}) }}
    <button class="btn" style="width: 80%;margin-left: 10%">{{ button_label|default('Update') }}</button>
    {{ form_end(form) }}



    <a href="{{ path('app_livre_index') }}">back to list</a>

    {{ include('livre/_delete_form.html.twig') }}
{% endblock %}
