{% extends 'base.html.twig' %}

{% block title %}PackJeton index{% endblock %}

{% block body %}
    <h1>PackJeton index</h1>

    <table class="table">
        <thead>
        <tr>
            <th></th>
            <th>DescriptionPack</th>
            <th>QuantiePack</th>
            <th>PrixPack</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for pack_jeton in pack_jetons %}
            <tr>
                <td><img src ="{{ asset('uploads/dicedreams-dicedream.gif') }}"  style = "width: 50px ;height: 50px"></td>
                <td>{{ pack_jeton.descriptionPack }}</td>
                <td>{{ pack_jeton.quantiePack }}</td>
                <td>{{ pack_jeton.prixPack }}</td>
                <td>
                    <a href="{{ path('app_pack_jeton_show', {'idPack': pack_jeton.idPack}) }}">show</a>
                    <a href="{{ path('app_pack_jeton_edit', {'idPack': pack_jeton.idPack}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_pack_jeton_new') }}">Create new</a>
{% endblock %}
