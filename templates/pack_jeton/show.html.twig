{% extends 'base_back.html.twig' %}

{% block title %}PackJeton{% endblock %}

{% block body %}

    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <h1>PackJeton</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPack</th>
                <td>{{ pack_jeton.idPack }}</td>
            </tr>
            <tr>
                <th>DescriptionPack</th>
                <td>{{ pack_jeton.descriptionPack }}</td>
            </tr>
            <tr>
                <th>QuantiePack</th>
                <td>{{ pack_jeton.quantiePack }}</td>
            </tr>
            <tr>
                <th>PrixPack</th>
                <td>{{ pack_jeton.prixPack }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pack_jeton_index') }}">back to list</a>

    <a href="{{ path('app_pack_jeton_edit', {'idPack': pack_jeton.idPack}) }}">edit</a>

    {{ include('pack_jeton/_delete_form.html.twig') }}
{% endblock %}
