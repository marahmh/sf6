<form method="post" action="{{ path('app_pack_jeton_delete', {'idPack': pack_jeton.idPack}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pack_jeton.idPack) }}">
    <button class="btn">Delete</button>
</form>
