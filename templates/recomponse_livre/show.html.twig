{% extends 'base_back.html.twig' %}

{% block title %}RecomponseLivre{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <h1>RecomponseLivre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Recomponse</th>
                <td>{{ recomponse_livre.idRecomponse.nomRecomponse }}</td>
            </tr>
            <tr>
                <th>Livre</th>
                <td>{{ recomponse_livre.idLivre }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ recomponse_livre.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recomponse_livre_index') }}">back to list</a>

    <a href="{{ path('app_recomponse_livre_edit', {'id': recomponse_livre.id}) }}">edit</a>

    {{ include('recomponse_livre/_delete_form.html.twig') }}
{% endblock %}
