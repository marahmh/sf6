<form method="post" action="{{ path('app_commentaire_livre_delete', {'idCommentaire': commentaire_livre.idCommentaire}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire_livre.idCommentaire) }}">
    <button class="btn">Delete</button>
</form>
